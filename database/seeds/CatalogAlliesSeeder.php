<?php

use App\Models\Catalog\Category;
use App\Models\Catalog\Specification;
use Illuminate\Database\Seeder;

class CatalogAlliesSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
/*         $category = Category::class->where('parent_id', '<>', 0)->pluck('id')->toArray();
        $specification = Specification::pluck('id')->toArray();

        factory()->make() */
    }
}
